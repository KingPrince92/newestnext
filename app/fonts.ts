import {Inter, Kanit} from "next/font/google";

export const kanit = Kanit({
	weight: "400",
	subsets: ["latin"],
	variable: "--font-kanit",
});

export const inter = Inter({
	weight: "400",
	subsets: ["latin"],
	variable: "--font-inter",
});
